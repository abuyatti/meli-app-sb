apiVersion: v1
kind: Service
metadata:
  name: meli-service
  labels:
    name: meli-service
    app: meli-app
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    name: meli-app-pod
    app: meli-app
